# AUDIT FIXES — Solutions for Findings 1, 2, 3 (2026-07-02)

**Sequence rule: one change at a time, WFO-gated, profit-first.** The running A/B HMM WFO stays valid as an apples-to-apples regime comparison; every number below gets re-measured on leak-free data afterwards.

---

## FIX 1 — HTF intra-bar lookahead (READY: `engine/regen_adx_asof.py`)

**Solution chosen:** rebuild every ADX/DI/band column **as-of each M15 row** — EWM state over COMPLETED bars + exactly one Wilder step with the PARTIAL forming bar. This equals what `mt5_data_updater` computes at live decision time, so backtest = train = live by construction. (Alternative "last closed bar only" was rejected: it would lag the live behavior that already trades on forming-bar values.)

**Validation already done:** vectorized algorithm vs brute-force partial resample on 5 spot timestamps — error exactly 0.0. Script also prints a leak-drift report (old vs as-of) per TF.

**Run order (after both A/B WFO windows finish):**
1. `backtest\Run_Fix1_AsOf_Regen.bat` — preview + drift report, then `--apply` (auto-backup).
2. Retrain + **rerun the WFO with the winning HMM variant → that total R is the new HONEST baseline.** +483.1R is retired; never compare against it again.
3. Same atomic switch: update `mt5_data_updater.update_adx` historical rows to the as-of convention (tell Claude — small patch, must ship together with the retrain).

**Expected effect:** headline WFO numbers will DROP. That is the fix working, not a regression.

---

## FIX 2 — Entry gate: broken calibration, dead features, value-destroying TRAIL

**2a. Prune dead weight (after WFO, with retrain of Fix 1):** remove the 10 zero-importance features (`hmm_state`-as-feature, `ts_bars_since_flip`, `momentum_aligned_1hr/4hr`, `mins_since_last_3star`, `adx_trend_count`, `h4_trending_h1_aligned`, `h4_ranging_h1_neutral`, `trade_direction`, `h4_in_ob_zone`) from the model feature lists; retire the SELL move-model (failed its own validation ρ=0.25) or retrain it behind the same PASS gate.

**2b. Probabilities:** stop treating `win_prob` as a probability until recalibrated. Concrete fix: calibrate on **rolling out-of-sample predictions** (each WFO week's predictions vs outcomes) instead of a single validation slice; re-check the bucket table (audit §5) after Fix 1 retrain. Threshold policy stays profit-first: the <50% bucket EARNED +113.7R, so do NOT raise thresholds; instead run a threshold sweep WFO (0.35 / 0.42 / 0.50) on leak-free data and keep whatever maximizes total R.

**2c. TRAIL exit (evidence-based):** TRAIL trades peak at **+0.94R** average and exit at **−0.15R** — 1.09R given back per trade; the 39 trades that reached ≥+0.5R still gave back 1.36R each. The tool for this already exists: `run_wfo.py --sweep-trails` retrains once per week and tests all 6 trail modes (line/off/after1r/be/htf/regime) on the same models. Run it once on the leak-free data; adopt the mode with the highest **total R**. (Also note FLIP gives back 0.66R avg from peak — the sweep covers that trade-off too.)

---

## FIX 3 — Live ≠ backtest divergence (TOOL READY: `engine/reconcile_shadow.py`)

**Quantified root split (June 2026):** (a) **entry-set gap** — only 8 of 66 backtest entries appear in the shadow log (12% overlap; 101 shadow-only, 58 backtest-only): live features/models/state at decision time differ from what the WFO assumes; (b) **exit-mechanics gap** — TRAIL is 49% of live exits vs 11% in backtest. Matched pairs (n=8) had similar R — so the gap is *which trades exist*, more than fill prices.

**Attack order (each step should visibly raise the overlap %):**
1. Fix 1 (features parity) — removes the decision-time feature skew.
2. Deploy HMM v3 winner (regime parity) — live "Volatile" mislabel changes thresholds/models per bar.
3. Trail parity audit — why does live trail fire 4× more often than backtest (config diff or bug-#F/#J residue)?
4. Fill audit — compare a week of demo fills vs modeled fills (spread 0.20, zero slippage assumption).

**Standing process (the actual solution):** run weekly —
`python reconcile_shadow.py --from <mon> --to <mon+7d> --backtest-glob "..\backtest\results\<current_wfo>\trades_*.csv" --out-dir "..\backtest\results\reconcile_<week>"`
Outputs one folder: `reconcile_summary.csv`, `matched_pairs.csv`, `backtest_only.csv`, `shadow_only.csv`. **Scaling gate: overlap high + exit mix similar + weekly R gap within ±20% for 4–8 consecutive weeks — before any capital increase.** Until then the audit recommendation stands: reduced risk (1–1.5%) + hard lot cap (live already printed 15.58 lots once). Risk % is Imtiyaz/Divyesh's call — 3% is the standing preference; the auditor's job is only to say what the data supports.

---

## Master sequence

| # | Action | Gate |
|---|---|---|
| 1 | A/B HMM WFO finishes (running now) | winner ≥ +483.1R (old-world comparison, both variants same data) |
| 2 | `Run_Fix1_AsOf_Regen.bat` → apply | drift report sane |
| 3 | Retrain + rerun WFO (winner variant) on as-of data | new HONEST baseline recorded |
| 4 | Feature prune + updater as-of patch + retrain | WFO not worse than #3 |
| 5 | `--sweep-trails` WFO on as-of data | adopt best total-R trail mode |
| 6 | Deploy to DEMO, restart bridge | verify_hmm_window PASS |
| 7 | Weekly `reconcile_shadow.py` | 4–8 weeks within ±20% → then scale |
